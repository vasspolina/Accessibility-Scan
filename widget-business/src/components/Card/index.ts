/* Existing imports say `from "./Card"`; with the flat Card.tsx deleted they
   resolve here, so ProSummary and ScoreGauge need no edit. */
export { Card } from "./Card";
export type { CardTone } from "./Card";
